import React from 'react';
import { Box } from '@mui/material';

const Chart = () => {
  return (
    <Box sx={{ maxHeight: '100vh', overflowY: 'auto' }}>
      {/* chart content */}
    </Box>
  );
};